# PalindromeMiniProject

An exercise that utilizes javascript to determine whether or not a user-inputted string of characters forms a palindrome, meaning it can be read the same forwards and backwards.
